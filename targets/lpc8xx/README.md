This directory holds family-common support shared by NXP LPC8xx targets.

Current shared content:
- Arm CMSIS core headers for Cortex-M0+

Per-device support lives in sibling target directories such as `targets/lpc824/`.
