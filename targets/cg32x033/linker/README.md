# CG32X033 Linker

The current CG32X033 path relies on ch32fun's generated linker script for the
`CH32X03x` family.

Add a dedicated linker script here only if the observed part/package needs a
memory layout that ch32fun does not already cover.
